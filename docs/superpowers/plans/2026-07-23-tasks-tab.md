# Вкладка «Задачи» (обзор всех задач) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить в приложение таб-бар на верхнем уровне и новый экран «Задачи» — обзор всех `DailyTask` по всем дням (read-only, с фильтром и поиском, клик по дню открывает существующую `DayDetailModal`).

**Architecture:** Один новый backend-эндпоинт `GET /tasks` в существующем `dailies`-модуле отдаёт плоский список всех задач с датой их дня. Фронт получает `TasksScreen` — self-contained `'use client'` компонент с собственным фетчем (как `CategoryBars`/`TaskTemplatesTab`). `Dashboard` становится шеллом с локальным стейтом `activeTab` (без роутинга) и переключает «Главный» ⇆ «Задачи». Фильтрация/группировка — клиентские.

**Tech Stack:** NestJS + Prisma (backend), Next.js App Router + React (frontend), Bun как рантайм/пакетный менеджер, Jest для тестов. Спека: `docs/superpowers/specs/2026-07-23-tasks-tab-design.md`.

## Global Constraints

- Пакетный менеджер/рантайм — **Bun** везде (`bun install` / `bunx jest` / `bun run build`), не npm/yarn.
- Даты в API сериализуются строками `YYYY-MM-DD` через `formatDate` (`backend/src/common/date.util.ts`) — никогда не отдавать `Date`-объекты.
- Фронтовые CSS-модули читают только дизайн-токены из `app/globals.css` (`--panel`, `--panel-alt`, `--border`, `--text`, `--text-muted`, `--text-dim`, `--accent`, `--accent-soft`, `--radius`, `--hair`, `--elev`, `--font-mono`). Хардкод цветов запрещён, UI-кит-библиотеки не подключаем.
- Сервисные тесты мокают `PrismaService` напрямую (`new DailiesService(mockPrisma, ...)`), без `@nestjs/testing`.
- Backend CORS залочен на два origin (`http://localhost:4887`, `https://tracker.performance:4888`) — новых способов достучаться до фронта не вводим, менять `main.ts` не нужно.
- Задачи read-only на этом экране: никаких мутаций (тоггл/удаление/правка текста) в `TasksScreen`. Правки — только через `DayDetailModal`.
- Коммиты частые, по одному на задачу; **без** trailer `Co-Authored-By`.

**Предусловие для тестов:** `bun install` уже выполнен в `backend/` и `frontend/` (см. CLAUDE.md). Backend-тесты запускаются из каталога `backend/`, фронтовые — из `frontend/`.

---

### Task 1: Backend — `getAllTasks()` + эндпоинт `GET /tasks`

**Files:**
- Modify: `backend/src/dailies/dailies.service.ts` (добавить интерфейс `TaskOverviewItem` и метод `getAllTasks`)
- Modify: `backend/src/dailies/dailies.controller.ts` (добавить роут `@Get('tasks')`)
- Test: `backend/src/dailies/dailies.service.spec.ts` (новый `describe` для `getAllTasks`)

**Interfaces:**
- Consumes: `PrismaService` (уже инжектится в `DailiesService`), `formatDate` из `../common/date.util` (уже импортирован в сервисе).
- Produces:
  ```ts
  export interface TaskOverviewItem {
    id: number;
    text: string;
    done: boolean;
    date: string;              // YYYY-MM-DD — день задачи
    carriedFromDate: string | null;
    carriedForward: boolean;
  }
  // DailiesService.getAllTasks(): Promise<TaskOverviewItem[]>
  // HTTP: GET /tasks -> TaskOverviewItem[]
  ```

- [ ] **Step 1: Написать падающий тест**

Добавить в конец `backend/src/dailies/dailies.service.spec.ts`:

```ts
describe('DailiesService.getAllTasks', () => {
  let service: DailiesService;
  let prisma: any;

  beforeEach(() => {
    prisma = { dailyTask: { findMany: jest.fn() } };
    service = new DailiesService(prisma, {} as any);
  });

  it('flattens every task with string dates, newest day first', async () => {
    prisma.dailyTask.findMany.mockResolvedValue([
      {
        id: 3,
        text: 'Созвон',
        done: false,
        order: 0,
        carriedForward: false,
        carriedFromDate: null,
        day: { date: new Date('2026-07-23T00:00:00.000Z') },
      },
      {
        id: 1,
        text: 'Почта',
        done: true,
        order: 1,
        carriedForward: false,
        carriedFromDate: new Date('2026-07-20T00:00:00.000Z'),
        day: { date: new Date('2026-07-21T00:00:00.000Z') },
      },
    ]);

    const result = await service.getAllTasks();

    expect(prisma.dailyTask.findMany).toHaveBeenCalledWith({
      include: { day: true },
      orderBy: [{ day: { date: 'desc' } }, { order: 'asc' }],
    });
    expect(result).toEqual([
      { id: 3, text: 'Созвон', done: false, date: '2026-07-23', carriedFromDate: null, carriedForward: false },
      { id: 1, text: 'Почта', done: true, date: '2026-07-21', carriedFromDate: '2026-07-20', carriedForward: false },
    ]);
  });
});
```

- [ ] **Step 2: Запустить тест — убедиться, что падает**

Run (из `backend/`): `bunx jest dailies.service.spec.ts -t getAllTasks`
Expected: FAIL — `service.getAllTasks is not a function`.

- [ ] **Step 3: Реализовать метод + интерфейс в сервисе**

В `backend/src/dailies/dailies.service.ts` рядом с существующим `export interface CarryCandidate { ... }` добавить:

```ts
export interface TaskOverviewItem {
  id: number;
  text: string;
  done: boolean;
  date: string;
  carriedFromDate: string | null;
  carriedForward: boolean;
}
```

И добавить метод в класс `DailiesService` (например, после `create`):

```ts
async getAllTasks(): Promise<TaskOverviewItem[]> {
  const tasks = await this.prisma.dailyTask.findMany({
    include: { day: true },
    orderBy: [{ day: { date: 'desc' } }, { order: 'asc' }],
  });
  return tasks.map((t) => ({
    id: t.id,
    text: t.text,
    done: t.done,
    date: formatDate(t.day.date),
    carriedFromDate: t.carriedFromDate ? formatDate(t.carriedFromDate) : null,
    carriedForward: t.carriedForward,
  }));
}
```

- [ ] **Step 4: Запустить тест — убедиться, что проходит**

Run (из `backend/`): `bunx jest dailies.service.spec.ts -t getAllTasks`
Expected: PASS.

- [ ] **Step 5: Добавить роут в контроллер**

В `backend/src/dailies/dailies.controller.ts` добавить метод в класс `DailiesController` (`Get` уже импортирован сверху):

```ts
@Get('tasks')
getAllTasks() {
  return this.dailiesService.getAllTasks();
}
```

- [ ] **Step 6: Прогнать весь спек-файл — ничего не сломалось**

Run (из `backend/`): `bunx jest dailies.service.spec.ts`
Expected: PASS (все `describe`, включая старые).

- [ ] **Step 7: Коммит**

```bash
git add backend/src/dailies/dailies.service.ts backend/src/dailies/dailies.controller.ts backend/src/dailies/dailies.service.spec.ts
git commit -m "feat(backend): GET /tasks — flat overview of all daily tasks across days"
```

---

### Task 2: Frontend — тип `TaskOverviewItem` + API-клиент `getAllTasks`

**Files:**
- Modify: `frontend/types/api.ts` (добавить интерфейс `TaskOverviewItem`)
- Modify: `frontend/lib/api.ts` (добавить `getAllTasks` + импорт типа)
- Test: `frontend/lib/api.spec.ts` (новый тест на `GET /tasks`)

**Interfaces:**
- Consumes: `request<T>` helper (внутренний в `api.ts`), тип из Task 1 (тот же контракт).
- Produces:
  ```ts
  // types/api.ts
  export interface TaskOverviewItem {
    id: number;
    text: string;
    done: boolean;
    date: string;
    carriedFromDate: string | null;
    carriedForward: boolean;
  }
  // lib/api.ts
  export function getAllTasks(): Promise<TaskOverviewItem[]>  // GET /tasks
  ```

- [ ] **Step 1: Написать падающий тест**

В `frontend/lib/api.spec.ts` заменить первую строку импорта на:

```ts
import { getAllTasks, getDay, updatePomodoros } from './api';
```

И добавить внутрь `describe('api request helper', () => { ... })` новый тест (перед закрывающей скобкой describe):

```ts
it('fetches all tasks from the /tasks endpoint', async () => {
  global.fetch = jest.fn().mockResolvedValue({
    ok: true,
    status: 200,
    json: async () => [
      { id: 1, text: 'x', done: false, date: '2026-07-23', carriedFromDate: null, carriedForward: false },
    ],
  }) as unknown as typeof fetch;

  const result = await getAllTasks();

  expect(global.fetch).toHaveBeenCalledWith(
    'http://localhost:3001/tasks',
    expect.objectContaining({ headers: expect.objectContaining({ 'Content-Type': 'application/json' }) }),
  );
  expect(result).toHaveLength(1);
});
```

- [ ] **Step 2: Запустить тест — убедиться, что падает**

Run (из `frontend/`): `bunx jest api.spec.ts -t "all tasks"`
Expected: FAIL — `getAllTasks` не экспортируется (`is not a function`).

- [ ] **Step 3: Добавить тип**

В `frontend/types/api.ts` после `CarryCandidate` (около строки 53) добавить:

```ts
export interface TaskOverviewItem {
  id: number;
  text: string;
  done: boolean;
  date: string;
  carriedFromDate: string | null;
  carriedForward: boolean;
}
```

- [ ] **Step 4: Добавить функцию в API-клиент**

В `frontend/lib/api.ts` добавить `TaskOverviewItem` в список импортируемых типов (блок `import type { ... } from '@/types/api';` вверху файла), затем добавить функцию рядом с остальными dailies-функциями (после `deleteDaily`):

```ts
export function getAllTasks(): Promise<TaskOverviewItem[]> {
  return request(`/tasks`);
}
```

- [ ] **Step 5: Запустить тест — убедиться, что проходит**

Run (из `frontend/`): `bunx jest api.spec.ts`
Expected: PASS (все тесты файла).

- [ ] **Step 6: Коммит**

```bash
git add frontend/types/api.ts frontend/lib/api.ts frontend/lib/api.spec.ts
git commit -m "feat(frontend): getAllTasks API client + TaskOverviewItem type"
```

---

### Task 3: Frontend — компонент `TasksScreen`

**Files:**
- Create: `frontend/components/TasksScreen.tsx`
- Create: `frontend/components/TasksScreen.module.css`

**Interfaces:**
- Consumes: `getAllTasks()` (Task 2), `TaskOverviewItem` (Task 2), `formatDisplayDate(dateStr)` и `formatOriginDate(originDate, targetDate)` из `@/lib/date`.
- Produces:
  ```ts
  interface TasksScreenProps {
    onSelectDate: (date: string) => void;   // открыть DayDetailModal на этом дне
    refreshKey: number;                       // бамп -> перечитать список
  }
  export default function TasksScreen(props: TasksScreenProps): JSX.Element
  ```

Компоненты в этом проекте юнит-тестами не покрываются — тестируется чистая логика в `lib/` (она уже покрыта в Task 1/2). Гейт этой задачи — успешный typecheck-билд (`bun run build`) + визуальная проверка в браузере.

- [ ] **Step 1: Создать компонент**

Создать `frontend/components/TasksScreen.tsx`:

```tsx
'use client';

import { useEffect, useMemo, useState } from 'react';
import type { TaskOverviewItem } from '@/types/api';
import { getAllTasks } from '@/lib/api';
import { formatDisplayDate, formatOriginDate } from '@/lib/date';
import styles from './TasksScreen.module.css';

type Filter = 'all' | 'pending' | 'done';

interface TasksScreenProps {
  onSelectDate: (date: string) => void;
  refreshKey: number;
}

interface DayGroup {
  date: string;
  total: number;
  done: number;
  tasks: TaskOverviewItem[];
}

const FILTERS: { key: Filter; label: string }[] = [
  { key: 'all', label: 'Все' },
  { key: 'pending', label: 'Невыполненные' },
  { key: 'done', label: 'Выполненные' },
];

export default function TasksScreen({ onSelectDate, refreshKey }: TasksScreenProps) {
  const [items, setItems] = useState<TaskOverviewItem[]>([]);
  const [loading, setLoading] = useState(true);
  const [filter, setFilter] = useState<Filter>('all');
  const [query, setQuery] = useState('');

  useEffect(() => {
    setLoading(true);
    getAllTasks()
      .then(setItems)
      .finally(() => setLoading(false));
  }, [refreshKey]);

  const groups = useMemo<DayGroup[]>(() => {
    const q = query.trim().toLowerCase();
    const byDate = new Map<string, DayGroup>();
    for (const item of items) {
      let group = byDate.get(item.date);
      if (!group) {
        group = { date: item.date, total: 0, done: 0, tasks: [] };
        byDate.set(item.date, group);
      }
      // Дневные счётчики считают ВСЕ задачи дня, независимо от активного фильтра,
      // чтобы "2/4" не врало при выбранном фильтре/поиске.
      group.total += 1;
      if (item.done) group.done += 1;

      if (filter === 'pending' && item.done) continue;
      if (filter === 'done' && !item.done) continue;
      if (q && !item.text.toLowerCase().includes(q)) continue;
      group.tasks.push(item);
    }
    // items приходят уже отсортированными (date desc / order asc), Map хранит порядок вставки.
    return Array.from(byDate.values()).filter((g) => g.tasks.length > 0);
  }, [items, filter, query]);

  if (loading) {
    return <div className={styles.loading}>загрузка…</div>;
  }

  const hasAnyTasks = items.length > 0;

  return (
    <div className={styles.screen}>
      <div className={styles.controls}>
        <h2 className={styles.heading}>Все задачи</h2>
        <div className={styles.filters}>
          {FILTERS.map((f) => (
            <button
              key={f.key}
              type="button"
              className={`${styles.filterBtn} ${filter === f.key ? styles.filterActive : ''}`}
              onClick={() => setFilter(f.key)}
            >
              {f.label}
            </button>
          ))}
        </div>
        <input
          className={styles.search}
          type="text"
          placeholder="Поиск…"
          value={query}
          onChange={(e) => setQuery(e.target.value)}
        />
      </div>

      {!hasAnyTasks && <div className={styles.empty}>Задач пока нет.</div>}
      {hasAnyTasks && groups.length === 0 && <div className={styles.empty}>Ничего не найдено.</div>}

      <div className={styles.groups}>
        {groups.map((group) => (
          <section key={group.date} className={styles.group}>
            <button type="button" className={styles.dayHeader} onClick={() => onSelectDate(group.date)}>
              <span className={styles.dayDate}>{formatDisplayDate(group.date)}</span>
              <span className={styles.dayCount}>
                {group.done}/{group.total}
              </span>
            </button>
            <ul className={styles.list}>
              {group.tasks.map((task) => (
                <li key={task.id} className={styles.item}>
                  <span className={`${styles.glyph} ${task.done ? styles.glyphDone : ''}`}>
                    {task.done ? '✓' : '☐'}
                  </span>
                  <span className={`${styles.text} ${task.done ? styles.textDone : ''}`}>{task.text}</span>
                  {task.carriedFromDate && (
                    <span className={styles.carriedBadge}>↻ {formatOriginDate(task.carriedFromDate, task.date)}</span>
                  )}
                </li>
              ))}
            </ul>
          </section>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Создать стили**

Создать `frontend/components/TasksScreen.module.css`:

```css
.screen {
  display: flex;
  flex-direction: column;
  gap: 18px;
}

.loading {
  color: var(--text-dim);
  font-size: 13px;
  padding: 24px 0;
}

.controls {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 12px;
}

.heading {
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text-muted);
  margin: 0;
  font-weight: 600;
  margin-right: auto;
}

.filters {
  display: flex;
  gap: 6px;
}

.filterBtn {
  background: var(--panel-alt);
  border: 1px solid var(--border);
  color: var(--text-muted);
  border-radius: 6px;
  padding: 5px 10px;
  font-size: 11.5px;
  cursor: pointer;
}

.filterBtn:hover {
  border-color: var(--accent);
  color: var(--accent);
}

.filterActive {
  background: var(--accent-soft);
  border-color: var(--accent);
  color: var(--accent);
}

.search {
  background: var(--panel-alt);
  border: 1px solid var(--border);
  border-radius: 6px;
  color: var(--text);
  padding: 6px 10px;
  font-size: 13px;
  min-width: 160px;
}

.search:focus {
  outline: none;
  border-color: var(--accent);
}

.groups {
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.group {
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 14px 16px 16px;
  box-shadow: inset 0 1px 0 var(--hair), var(--elev);
}

.dayHeader {
  display: flex;
  align-items: baseline;
  gap: 10px;
  width: 100%;
  background: none;
  border: none;
  padding: 0 0 10px;
  cursor: pointer;
  text-align: left;
}

.dayDate {
  font-size: 13px;
  color: var(--text);
  text-transform: capitalize;
  font-weight: 600;
  margin-right: auto;
}

.dayHeader:hover .dayDate {
  color: var(--accent);
}

.dayCount {
  font-family: var(--font-mono);
  font-size: 11.5px;
  color: var(--text-dim);
}

.list {
  list-style: none;
  margin: 0;
  padding: 0;
}

.item {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 6px 0;
  border-bottom: 1px solid var(--border);
}

.item:last-child {
  border-bottom: none;
}

.glyph {
  width: 16px;
  flex-shrink: 0;
  text-align: center;
  font-size: 12px;
  color: var(--text-dim);
}

.glyphDone {
  color: var(--accent);
}

.text {
  flex: 1;
  font-size: 13.5px;
  color: var(--text);
}

.textDone {
  color: var(--text-dim);
  text-decoration: line-through;
}

.carriedBadge {
  font-size: 10.5px;
  color: var(--text-dim);
  white-space: nowrap;
  margin-left: 4px;
}

.empty {
  color: var(--text-dim);
  font-size: 12.5px;
  font-style: italic;
  padding: 8px 0;
}
```

- [ ] **Step 3: Проверить typecheck-билд**

Run (из `frontend/`): `bun run build`
Expected: билд проходит без TS-ошибок (компонент компилируется; он пока нигде не отрендерен — это нормально, подключим в Task 4).

- [ ] **Step 4: Коммит**

```bash
git add frontend/components/TasksScreen.tsx frontend/components/TasksScreen.module.css
git commit -m "feat(frontend): TasksScreen — read-only overview of all tasks grouped by day"
```

---

### Task 4: Frontend — таб-бар и подключение `TasksScreen` в `Dashboard`

**Files:**
- Modify: `frontend/components/Dashboard.tsx` (стейт табов + таб-бар + условный рендер + рефреш-проводка)
- Modify: `frontend/components/Dashboard.module.css` (стили таб-бара, замена `.sysline` на `.sysHint`)

**Interfaces:**
- Consumes: `TasksScreen` (Task 3), существующие `setSelectedDate`, `refreshHistory`, `DayDetailModal`.
- Produces: пользовательский таб-переключатель «Главный» ⇆ «Задачи»; никакого нового публичного API.

- [ ] **Step 1: Импорт и константа табов**

В `frontend/components/Dashboard.tsx` добавить импорт рядом с остальными компонентами:

```tsx
import TasksScreen from './TasksScreen';
```

И перед `export default function Dashboard()` (рядом с `const HISTORY_LIMIT = 84;`) добавить:

```tsx
const TABS = [
  { key: 'home', label: 'Главный' },
  { key: 'tasks', label: 'Задачи' },
] as const;

type TabKey = (typeof TABS)[number]['key'];
```

- [ ] **Step 2: Добавить стейт**

В теле `Dashboard`, рядом с остальными `useState` (после `const [selectedDate, setSelectedDate] = useState<string | null>(null);`), добавить:

```tsx
const [activeTab, setActiveTab] = useState<TabKey>('home');
const [tasksRefreshKey, setTasksRefreshKey] = useState(0);
```

- [ ] **Step 3: Заменить `sysline` на таб-бар и обернуть контент**

В `return (...)` заменить блок

```tsx
    <div className={styles.wrap}>
      <div className={styles.sysline}>sys / daily-tracker</div>
      <Header
```

… целиком (от `<div className={styles.sysline}>` до закрывающего `</StatsPanel>`-блока) на структуру ниже. Конкретно: строку `sysline` заменяем на таб-бар, а весь «домашний» контент (`<Header .../>` + `<div className={styles.grid}>…</div>` + `<StatsPanel .../>`) заворачиваем в `activeTab === 'home'`, и добавляем ветку `activeTab === 'tasks'`:

```tsx
    <div className={styles.wrap}>
      <div className={styles.topbar}>
        <nav className={styles.tabBar}>
          {TABS.map((t) => (
            <button
              key={t.key}
              type="button"
              className={`${styles.tab} ${activeTab === t.key ? styles.tabActive : ''}`}
              onClick={() => setActiveTab(t.key)}
            >
              {t.label}
            </button>
          ))}
        </nav>
        <span className={styles.sysHint}>sys / daily-tracker</span>
      </div>

      {activeTab === 'home' && (
        <>
          <Header
            dateLabel={formatDisplayDate(date)}
            streak={streak}
            pomodoroStreakMin={pomodoroStreakMin}
            pomodoroStreakOpt={pomodoroStreakOpt}
            notificationsEnabled={notificationsActive}
            onEnableNotifications={enableNotifications}
            onOpenSettings={() => setSettingsOpen(true)}
          />
          <div className={styles.grid}>
            <SpheresPanel
              categories={day.categories}
              eveningClosed={day.eveningClosed}
              rating={day.rating}
              comment={day.comment}
              onToggle={toggleCategory}
              onToggleEveningClosed={toggleEveningClosed}
              onRatingChange={changeRating}
              onCommentChange={changeComment}
            />
            <DailiesPanel
              date={date}
              dailies={day.dailies}
              onAdd={addDailyTask}
              onToggle={toggleDaily}
              onDelete={deleteDailyTask}
              onCarry={carryDailyTasks}
            />
            <YoutubePanel
              minutes={day.youtubeMinutes}
              budget={settings.youtubeBudget}
              onAdd={addYoutubeMinutes}
              onReset={resetYoutube}
              onBudgetChange={changeYoutubeBudget}
            />
            <PomodoroPanel count={day.pomodoros} onAdd={addPomodoro} onReset={resetPomodoro} />
          </div>
          <StatsPanel history={history} onSelectDate={setSelectedDate} />
        </>
      )}

      {activeTab === 'tasks' && <TasksScreen onSelectDate={setSelectedDate} refreshKey={tasksRefreshKey} />}
```

(Модалки `settingsOpen`/`selectedDate` ниже остаются на месте — они уже вне заменяемого блока; в следующем шаге правим только `onDataChanged`.)

- [ ] **Step 4: Прокинуть рефреш задач в модалку дня**

В том же `return` найти рендер `DayDetailModal` и заменить его `onDataChanged`, чтобы правки прошлого дня из таба «Задачи» перечитывали список:

Было:
```tsx
      {selectedDate && (
        <DayDetailModal date={selectedDate} onClose={() => setSelectedDate(null)} onDataChanged={refreshHistory} />
      )}
```

Стало:
```tsx
      {selectedDate && (
        <DayDetailModal
          date={selectedDate}
          onClose={() => setSelectedDate(null)}
          onDataChanged={() => {
            refreshHistory();
            setTasksRefreshKey((k) => k + 1);
          }}
        />
      )}
```

- [ ] **Step 5: Стили таб-бара**

В `frontend/components/Dashboard.module.css` заменить блок `.sysline { ... }` на:

```css
.topbar {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 18px;
}

.tabBar {
  display: flex;
  gap: 6px;
  margin-right: auto;
}

.tab {
  background: var(--panel-alt);
  border: 1px solid var(--border);
  color: var(--text-muted);
  border-radius: 7px;
  padding: 6px 14px;
  font-size: 13px;
  cursor: pointer;
}

.tab:hover {
  border-color: var(--accent);
  color: var(--accent);
}

.tabActive {
  background: var(--accent-soft);
  border-color: var(--accent);
  color: var(--accent);
}

.sysHint {
  font-family: var(--font-mono);
  font-size: 10.5px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--text-dim);
  white-space: nowrap;
}
```

- [ ] **Step 6: Проверить typecheck-билд**

Run (из `frontend/`): `bun run build`
Expected: билд проходит без ошибок.

- [ ] **Step 7: Прогнать фронтовые тесты (регрессия)**

Run (из `frontend/`): `bunx jest`
Expected: PASS (все существующие + `api.spec.ts` из Task 2).

- [ ] **Step 8: Ручная проверка в браузере**

Поднять стек (`docker compose up -d --build`), открыть http://localhost:4887. Проверить:
- Сверху вместо `sys / daily-tracker` — таб-бар «Главный» / «Задачи», sysline мелкой подписью справа.
- «Главный» показывает прежний экран без изменений.
- «Задачи» показывает все задачи по дням, фильтр «Все/Невыполненные/Выполненные» и поиск работают, счётчик дня `done/total` корректен.
- Клик по заголовку дня открывает `DayDetailModal`; правка задачи в модалке и закрытие → список на табе «Задачи» обновился.

- [ ] **Step 9: Коммит**

```bash
git add frontend/components/Dashboard.tsx frontend/components/Dashboard.module.css
git commit -m "feat(frontend): top-level tab bar, Tasks tab wired into Dashboard"
```

---

## Self-Review

**Spec coverage:**
- Таб-бар на уровне sysline, sysline → мелкая подпись → Task 4 (steps 3, 5). ✅
- Без роутинга, `activeTab` в `Dashboard` → Task 4. ✅
- Расширяемый массив табов (`TABS`) → Task 4 step 1. ✅
- `GET /tasks` в `dailies`-модуле, плоский вид, `date desc / order asc`, сериализация через `formatDate` → Task 1. ✅
- `TasksScreen` self-contained, фетч на маунте, пропсы `onSelectDate`/`refreshKey` → Task 3. ✅
- Фильтр «Все/Невыполненные/Выполненные» + поиск, клиентские → Task 3. ✅
- Группировка по дню, счётчик `done/total` по всем задачам дня, бейдж переноса → Task 3. ✅
- Клик по дню → существующая `DayDetailModal`; рефреш списка после правок (`tasksRefreshKey`) → Task 4 steps 3–4. ✅
- Read-only (никаких мутаций на экране) → соблюдено в Task 3 (только рендер). ✅
- Тип `TaskOverviewItem` + `getAllTasks` API-клиент → Task 2. ✅
- Тесты: backend `getAllTasks` (Task 1), frontend `api.spec` (Task 2). ✅
- Не входит в объём (GTD, серверные фильтры, перенос шестерёнки, YouTube) → не реализуется. ✅

**Placeholder scan:** плейсхолдеров/«TBD»/«обработать ошибки»-заглушек нет — весь код и все команды приведены полностью.

**Type consistency:** `TaskOverviewItem` идентичен в Task 1 (backend) и Task 2 (frontend). `getAllTasks(): Promise<TaskOverviewItem[]>` совпадает в api-клиенте и в потреблении внутри `TasksScreen`. Пропсы `TasksScreen` (`onSelectDate`, `refreshKey`) совпадают с местом вызова в `Dashboard` (Task 4 step 3). Классы CSS, используемые в `TasksScreen.tsx`, все определены в `TasksScreen.module.css`; классы `.topbar/.tabBar/.tab/.tabActive/.sysHint` — в `Dashboard.module.css`.
